import 'package:flutter/material.dart';

class DesignTokens {
  static const s1 = 4.0,
      s2 = 8.0,
      s3 = 12.0,
      s4 = 16.0,
      s5 = 20.0,
      s6 = 24.0,
      s7 = 32.0;

  // radius
  static const radiusChip = 16.0;
  static const radiusCard = 20.0;
  static const radiusCardSmall = 12.0;
  static const radiusCardLarge = 24.0;

  // Background Colors
  static const bg = Color(0xFFF2EEF9);
  static const bgWhite = Colors.white;
  static const bgBlack = Colors.black;

  // Text Colors
  static const text = Color(0xFF0F1115);
  static const textPrimary = Color(0xFF1A1A1A);
  static const textSecondary = Color(0xFF666666);
  static const textWhite = Colors.white;
  static const textGrey = Color(0xFFCCCCCC);

  // Card Colors
  static const cardYellow = Color(0xFFFFC85D);
  static const cardBlue = Color(0xFFBBD2FF);
  static const cardRed = Color(0xFFFF6B6B);
  static const cardTeal = Color(0xFF4ECDC4);
  static const cardOrange = Color(0xFFFF6B35);

  // Challenge Card Colors
  static const challengeGradientStart = Color(0xFFE8E2F3);
  static const challengeGradientEnd = Color(0xFFF0EBF7);

  // Metrics Card Colors
  static const metricGreen = Color(0xFFE7F8ED);
  static const metricBlue = Color(0xFFBBD2FF);
  static const metricOrange = Color(0xFFFFEED6);

  // Difficulty Colors
  static const difficultyLight = Colors.green;
  static const difficultyMedium = Colors.orange;
  static const difficultyHard = Colors.red;

  // Social Media Colors
  static const socialPink = Colors.pink;
  static const socialRed = Colors.red;
  static const socialBlue = Colors.blue;

  // Shadow Colors
  static const shadowLight = Color(0x0A000000);
  static const shadowMedium = Color(0x1A000000);
  static const shadowDark = Color(0x33000000);

  // Border Colors
  static const borderLight = Color(0xFFFFF0F0);
  static const borderGrey = Color(0xFFE0E0E0);

  // Icons Colors
  static const iconGrey = Color(0xFF666666);
  static const iconLightGrey = Color(0xFFE0E0E0);
}
